/*$(document).ready(function(){
  $(".mainCarousel").slick({
    setting-name: setting-value
  });
});*/

$(".mainCarousel").slick({
  dots: true,
  infinite: true,
  speed: 500,
  fade: true,
  cssEase: 'linear'
});